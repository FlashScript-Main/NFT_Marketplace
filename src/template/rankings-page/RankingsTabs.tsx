import { SectionToScroll } from "@/animations/ScrollAnimations"

const RankingsTabs = ({ locale }: { locale: string }) => {

    return (
        <SectionToScroll className={` |  |  | border-2 border-indigo-500`}>
            <div className={` |  |  | `}>
                
            </div>
        </SectionToScroll>
    )

}

export default RankingsTabs